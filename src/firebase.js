import { initializeApp } from 'firebase/app';

const firebaseConfig = {};// TODO: Add your Firebase project config object here



export const firebaseApp = initializeApp(firebaseConfig);
export default firebaseApp;
